# Install the local SDK package together with its declared dependencies.
./f1-sdk

# API application runtime dependencies.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Explicit SDK runtime dependencies for simpler installs and visibility.
httpx>=0.27.0
pydantic>=2.6.0
paho-mqtt>=2.1.0
